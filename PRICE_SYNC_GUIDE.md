# 电价数据自动同步说明

## 功能说明
应用启动时会自动检查 `blogapp/data/hourly_avg_30days(1).xlsx` 文件，如果数据库中的电价数据与文件不同，会自动更新数据库。

## 更新的文件
1. `blogapp/data/hourly_avg_30days(1).xlsx` - 更新了代理公司电价数据，使其与电网价格更有竞争性
2. `blogapp/utils/price_sync.py` - 新增自动同步工具
3. `blogapp/__init__.py` - 集成了启动时自动同步功能

## 新电价策略
- 低谷时段 (00-06时): 0.26-0.31 元/kWh (比电网0.41便宜)
- 高峰时段 (08-11, 18-22时): 0.65-1.10 元/kWh (比电网0.74贵)
- 平时时段: 约0.55 元/kWh (比电网0.57略便宜)

## 队友同步方法
```bash
cd software_project_2
git pull
# 重启应用，数据会自动同步
python microblog.py
```

应用启动时会在日志中显示：
```
============================================================
检查电价数据...
✅ 电价数据已更新
============================================================
```

## 手动强制更新（如需要）
如果需要手动强制更新数据库：
```bash
cd software_project_2
python import_electricity_prices.py
```
